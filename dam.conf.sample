
endpoint=https://ELASTIC-OR-OPENSEARCH-HOST:9200

user=HTTP_USER

passwd=HTTP_PASSWD

webhook=https://hooks.slack.com/services/...

svc_webhook=https://hooks.slack.com/services/...

# quick helper to disable all alerts at once - overrides alert.conf dummy variable
dummy=1

